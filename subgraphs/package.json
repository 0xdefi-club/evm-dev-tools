{
  "name": "subgraphs",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ 0xdeficlub/subgraphs",
    "create-local": "graph create --node http://localhost:8020/ 0xdeficlub/subgraphs",
    "remove-local": "graph remove --node http://localhost:8020/ 0xdeficlub/subgraphs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xdeficlub/subgraphs",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.71.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
